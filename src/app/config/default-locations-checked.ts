import LocationsChecked from '../locations-checked.type';

const DEFAULT_LOCATION_CHECKED: LocationsChecked = {
  federal: false,
  provincial: true,
  regional: false,
};

export default DEFAULT_LOCATION_CHECKED;
